{
  "extends": ["@linode/tsconfig/react-package", "@linode/tsconfig/non-strict"],
  "compilerOptions": {
    "noEmit": true, // We don't need type declorations emitted because we treat @linode/queries as a local-only package right now
  },
  "include": ["src"]
}
