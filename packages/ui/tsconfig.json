{
  "extends": "@linode/tsconfig/react-package",
  "compilerOptions": {
    "noEmit": true, // We don't need type declorations emitted because we treat @linode/ui as a local-only package right now
  },
  "include": ["src"],
}
