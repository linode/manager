{
  "extends": "@linode/tsconfig/package",
  "compilerOptions": {
    "noEmit": true, // We don't need type declorations emitted because we treat @linode/search as a local-only package right now
  },
  "include": ["src"]
}
