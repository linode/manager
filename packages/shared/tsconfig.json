{
  "extends": ["@linode/tsconfig/react-package", "@linode/tsconfig/non-strict"],
  "compilerOptions": {
    "noEmit": true, // We don't need type declorations emitted because we treat @linode/shared as a local-only package right now
    "types": ["@testing-library/jest-dom"]
  },
  "include": ["src"]
}
