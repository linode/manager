---
"@linode/manager": Upcoming Features
---

ClouPulse: Remove `tags filter` from linode filter config and rename resources to `Linode Label(s)` in CloudPulse dashboards ([#12192](https://github.com/linode/manager/pull/12192))
