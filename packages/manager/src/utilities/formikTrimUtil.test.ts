// import { handleFormikBlur } from "./formikTrimUtil";
// import { Formik, useFormik } from "formik";

// const setFieldError = jest.fn();
// const setError = jest.fn();
// const formik = useFormik();

// describe('handleFormikBlur', () => {
//   it('should trim trailing space from the field', () => {
//     handleFormikBlur(e, formik)
//   });

//   it('should trim leading space from the field, () => {
//     handleAPIErrors(errorWithoutField, setFieldError, setError);
//     expect(setFieldError).not.toHaveBeenCalledWith();
//     expect(setError).toHaveBeenCalledWith(errorWithoutField[0].reason);
//   });
// });
