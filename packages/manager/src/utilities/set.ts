// oh boy oh boy fear

/**
 * Helper to set the given value at the given path of the given object.
 * This util is a direct replacement for `set` from lodash. [maybe we will see i'm kinda afraid ngl]
 *
 * @param object — The object to modify.
 * @param path — The path of the property to set.
 * @param value — The value to set.
 * @return — Returns object.
 */
export const set = () => {
  // must check for proto / constructor / prototype
  return;
};
