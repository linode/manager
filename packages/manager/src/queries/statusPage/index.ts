/**
 * These are types and methods for working with Linode's statuspage.io integration
 * (status.linode.com). Since these are not part of the Linode API, and are only
 * used through this query, all of this is contained within src/queries.
 */

export * from './statusPage';
export * from './types';
