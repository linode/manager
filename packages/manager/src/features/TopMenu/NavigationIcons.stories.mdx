import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import UserIcon from 'src/assets/icons/account.svg';
import Community from 'src/assets/icons/community_nav.svg';
import HelpIcon from 'src/assets/icons/get_help.svg';
import Bell from 'src/assets/icons/notification.svg';
import Grid from 'src/components/Grid';
import { makeStyles } from 'src/components/core/styles';

<Meta title="core/Icons/Navigation Icons" />

export const useStyles = makeStyles(() => ({
  item: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    width: 125,
    '& svg': {
      width: 30,
      height: 30,
    },
  },
  label: {
    fontSize: '0.875rem',
    margin: 8,
    textAlign: 'center',
  },
}));

export const navIcons = {
  'Help & Support': <HelpIcon />,
  'Linode Cloud Community': <Community />,
  Notifications: <Bell />,
  Account: <UserIcon />,
}

export const NavigationIcons = () => {
  const classes = useStyles();
  return (
    <Grid container>
      {
        Object.entries(navIcons).map(([iconLabel, icon]) => {
          return (
            <Grid key={iconLabel} item className={classes.item}>
              {icon}
              <div className={classes.label}>{iconLabel}</div>
            </Grid>
          )
        })
      }
    </Grid>
  )
};

# Navigation Icons

<Canvas>
  <NavigationIcons />
</Canvas>