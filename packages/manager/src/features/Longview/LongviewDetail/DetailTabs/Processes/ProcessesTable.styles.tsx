import { styled } from '@mui/material/styles';

export const StyledDiv = styled('div', { label: 'StyledDiv' })({
  alignItems: 'center',
  display: 'flex',
  flexFlow: 'row nowrap',
  wordBreak: 'break-all',
});
