import * as React from 'react';

import { Paper } from 'src/components/Paper';

export const Namespaces = () => {
  return <Paper>Namespaces Content here</Paper>;
};

export default React.memo(Namespaces);
