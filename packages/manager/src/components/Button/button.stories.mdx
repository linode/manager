import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import Button from 'src/components/Button';

<Meta
  title="UI Elements/Button"
  component={Button}
  argTypes={{
    buttonType: {
      description: 'The variant to use.',
      <!-- Keeping this as an example for labelling a prop as required: -->
      <!-- type: { required: true }, -->
    },
    compact: {
      description: 'Insert description here.',
      table: { defaultValue: { summary: false } },
    },
    disabled: {
      table: { defaultValue: { summary: false } },
    },
    disableRipple: {
      table: { defaultValue: { summary: false } },
    },
    fullWidth: {
      table: { defaultValue: { summary: false } },
    },
    loading: {
      description: 'Insert description here.',
      table: { defaultValue: { summary: false } },
    },
    tooltipText: {
      description: 'Insert description here.',
      table: { defaultValue: { summary: false } },
    },
  }}
/>

# Button

Buttons allow users to take actions, and make choices, with a single tap.

export const Template = (args) => <Button {...args}>I'm a basic button</Button>;

<Canvas>
  <Story name="Basic">{Template.bind({})}</Story>
</Canvas>

### Default

<Canvas>
  <Story name="Default">
    <Button buttonType="primary" data-qa-button="primary">
      Primary
    </Button>
    <Button buttonType="secondary" data-qa-button="secondary">
      Secondary
    </Button>
    <Button buttonType="outlined" data-qa-button="secondary">
      Outlined
    </Button>
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story name="Disabled">
    <Button buttonType="primary" disabled data-qa-button="primary">
      Primary
    </Button>
    <Button buttonType="secondary" disabled data-qa-button="secondary">
      Secondary
    </Button>
    <Button buttonType="outlined" disabled data-qa-button="secondary">
      Outlined
    </Button>
  </Story>
</Canvas>

### Loading

<Canvas>
  <Story name="Loading">
    <Button buttonType="primary" loading data-qa-button="primary">
      Primary
    </Button>
    <Button buttonType="secondary" loading data-qa-button="secondary">
      Secondary
    </Button>
    <Button buttonType="outlined" loading data-qa-button="secondary">
      Outlined
    </Button>
  </Story>
</Canvas>

<ArgsTable
  story="Basic"
  sort="requiredFirst"
  exclude={[
    <!-- These are props that are not relevant -->
    'children',
    'className',
    'color',
    'disableElevation',
    'disableFocusRipple',
    'endIcon',
    'href',
    'size',
    'startIcon',
    'tabIndex',
    'variant',
    <!-- These are props inherited from ButtonBase that we do not use -->
    'ref',
    'action',
    'centerRipple',
    'disableTouchRipple',
    'focusRipple',
    'focusVisibleClassName',
    'onFocusVisible',
    'TouchRippleProps',
  ]}
/>
