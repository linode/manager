import PasswordInput from 'src/components/PasswordInput';
import { Canvas, Meta, Story, Source } from '@storybook/addon-docs';

<Meta title="UI Elements/Password Input" component={PasswordInput} />

export const Template = (args, context) => {
  return (
    <PasswordInput
      {...args}
      value={context.value}
      onChange={context.onChange}
    />
  );
};

# Password Input

<Canvas>
  <Story
    name="Password Input"
    args={{ value: 'Hello' }}
    decorators={[
      (Story, context) => {
        const [value, setValue] = React.useState('');
        return Story({ value, onChange: (e) => setValue(e.target.value) });
      },
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>
