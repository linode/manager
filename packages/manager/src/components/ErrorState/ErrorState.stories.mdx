import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { ErrorState } from 'src/components/ErrorState/ErrorState';

<Meta
  title="Components/Error State"
  component={ErrorState}
  args={{
    errorText: 'An error has occurred.',
    compact: false,
    cozy: false,
  }}
  argTypes={{
    CustomIcon: {
      control: false,
      description: 'An SVG to display in place of the error icon.',
    },
    CustomIconStyles: {
      control: false,
      description: 'CSS properties to apply to the custom icon.'
    },
    compact: {
      description: 'Reduces the padding on the root element.',
    },
    cozy: {
      description: 'Minimal padding on the root element.',
    },
  }}
/>

export const ErrorStateTemplate = (args, context) => <ErrorState {...args} />;

# Error State

### Usage
Shown when a page or component fails to load.

<Canvas>
  <Story name="Error State">
    { ErrorStateTemplate.bind({}) }
  </Story>
</Canvas>

<ArgsTable story="Error State" sort="requiredFirst" exclude={['className']} sort="requiredFirst"/>
