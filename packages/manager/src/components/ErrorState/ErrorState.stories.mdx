import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import ErrorState from './ErrorState';

<Meta
  title="Features/Entity Landing Page/Error Display"
  component={ErrorState}
  args={{
    errorText: 'An error has occured.',
    compact: false,
    cozy: false,
  }}
  argTypes={{
    CustomIcon: {
      control: false,
      description: 'A SVG to display in place of the error icon.',
    },
    CustomIconStyles: {
      control: false,
      description: 'CSS properties to apply to the custom icon.'
    },
    compact: {
      description: 'Reduces the padding on the root element.',
    },
    cozy: {
      description: 'Minimal padding on the root element.',
    },
  }}
/>

export const ErrorStateTemplate = (args, context) => <ErrorState {...args} />;

# Error Display

### Usage
Shown when a page or component fails to load.

<Canvas>
  <Story name="Error Display">
    { ErrorStateTemplate.bind({}) }
  </Story>
</Canvas>

<ArgsTable story="Error Display" sort="requiredFirst" exclude={['className']}/>
