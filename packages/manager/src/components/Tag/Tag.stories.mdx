import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import Tag from './Tag';

<Meta title="Elements/Tag" component={Tag} />

# Tag

export const Template = (args) => <Tag label="tag" {...args} />;

<Canvas>
  <Story
    name="Tag"
    args={{
      label: 'tag',
      maxLength: null,
    }}
    argTypes={{
      label: {
        description: 'The content of the label.',
        type: { required: true },
        table: {
          type: { summary: 'string' },
        },
      },
      colorVariant: {
        description: 'The variant to use.',
        control: {
          type: 'radio',
        },
        options: ['default', 'blue', 'lightBlue'],
      },
      component: {
        description:
          'The component used for the root node. Either a string to use a HTML element or a component.',
        table: {
          type: { summary: 'elementType' },
        },
        control: { type: null },
      },
      maxLength: {
        description: 'Must be greater than 4.',
        table: {
          type: { summary: 'number' },
        },
        control: {
          type: 'number',
          min: 4,
        },
      },
      onDelete: {
        description:
          'Override the default delete icon element. Shown only if onDelete is set.',
        table: {
          type: { summary: '() => void' },
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Tag" sort="requiredFirst" />
