import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import Gauge from './GaugePercent';
import Typography from 'src/components/core/Typography';

<Meta title="Components/Analytics & Graphs" component={Gauge} />

# 📈 Gauges

- **Chart.js** is the charting tool we use for gauges
- Best used to show percentages

export const Template = (args) => <Gauge {...args} />;

<Canvas>
  <Story
    name="Gauge"
    args={{
      value: 50,
      max: 200,
      height: 150,
      width: 150,
      innerText: '25%',
      innerTextFontSize: 12,
      subTitle: 'CPU',
      filledInColor: undefined,
      nonFilledInColor: undefined,
    }}
    argTypes={{
      value: {
        description:
          'The value that is displayed by the gauge. This value should be <= `max`',
        type: { summary: 'number', required: true },
      },
      max: {
        description: 'The max value of the gauge',
        type: { summary: 'number', required: true },
      },
      height: {
        description: 'The height of the gauge',
        type: { summary: 'number' },
        table: {
          defaultValue: {
            summary: 300,
          },
        },
      },
      width: {
        description: 'The width of the gauge',
        type: { summary: 'number | string' },
        table: {
          defaultValue: {
            summary: 300,
          },
        },
      },
      innerText: {
        description: 'Text that shows up inside (in the middle) of the gauge',
        type: { summary: 'string' },
      },
      innerTextFontSize: {
        description: 'The font size in `px` of the inner text',
        type: { summary: 'number' },
      },
      subTitle: {
        description: 'A subtitle that appears below the gauge',
        type: { summary: 'JSX.Element | string' },
      },
      filledInColor: {
        description: 'The color for the filled in portion of the gauge',
        type: { summary: 'string' },
      },
      nonFilledInColor: {
        description: 'The color for the non-filled in portion of the gauge',
        type: { summary: 'string' },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Gauge" />
