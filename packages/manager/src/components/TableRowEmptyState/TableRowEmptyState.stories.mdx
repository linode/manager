import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import TableBody from 'src/components/core/TableBody';
import TableHead from 'src/components/core/TableHead';
import Table from 'src/components/Table';
import TableCell from 'src/components/TableCell';
import TableRow from 'src/components/TableRow';
import TableRowEmptyState from 'src/components/TableRowEmptyState';

<Meta title="Components/Table" component={TableRowEmptyState} />

# Table Row Empty State

- Tables that do not yet have any data should be shown full width with the title and column heads. This let’s users know where data will appear when it is available.
- An empty table should have language that clearly says why the table has no data and what action to take to populate with data.

export const Template = (args) => {
  const fields = [];
  for (let i = 0; i < args.colSpan; i++) {
    fields.push(
      <TableCell>
        Label {i}
      </TableCell>
    );
  }
  return (
    <Table>
      <TableHead>
        <TableRow>
          {fields}
        </TableRow>
      </TableHead>
      <TableBody>
        <TableRowEmptyState {...args} />
      </TableBody>
    </Table>
  );
}

<Canvas>
  <Story
    name="Table Row Empty State"
    args={{
      colSpan: 3
    }}
    argTypes={{
      colSpan: {
        description: "How many columns the empty state should span",
      },
      message: {
        description: "The text that displays in the table",
        table: {
          defaultValue: {
            summary: 'No items to display.',
          },
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Table Row Empty State" />