import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { distroIcons } from '/src/components/ImageSelect/icons';
import 'font-logos/assets/font-logos.css';

<Meta title="core/Icons/Distribution Icons" />

export const DistributionIcons = () => {
    return (
        Object.entries(distroIcons).map(([iconLabel, icon]) => {
            return (
                <div key={icon} className={`fl-${icon}`}>{iconLabel}</div>
            )
        })
    )
};

# Distribution Icons

<Canvas>
  <DistributionIcons />
</Canvas>