import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { makeStyles } from 'src/components/core/styles';
import { distroIcons } from 'src/components/ImageSelect/icons';
import Grid from 'src/components/Grid';
import 'font-logos/assets/font-logos.css';

<Meta title="Core Styles/Icons/Distribution Icons" />

export const useStyles = makeStyles(() => ({
  item: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    width: 125,
    '& span': {
      fontSize: '40px',
    },
  },
  label: {
    fontSize: '0.875rem',
    margin: 8,
  },
}));

export const DistributionIcons = () => {
  const classes = useStyles();
  return (
    <Grid container>
      {Object.entries(distroIcons).map(([iconLabel, icon]) => {
        return (
          <Grid key={iconLabel} item className={classes.item}>
            <span className={`fl-${icon}`}></span>
            <div className={classes.label}>{iconLabel}</div>
          </Grid>
        );
      })}
    </Grid>
  );
};

# Distribution Icons

<Canvas>
  <DistributionIcons />
</Canvas>
