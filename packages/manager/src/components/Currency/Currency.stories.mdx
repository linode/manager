import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import Currency from './Currency';

<Meta title="Core/Typography/Currency" component={Currency} />

export const CurrencyTemplate = (args) => (
  <Currency {...args} />
)

# Currency

<Canvas>
  <Story 
    name="Currency"
    args={{
      quantity: 4.51,
      decimalPlaces: 2,
      wrapInParentheses: false,
    }}>
      {CurrencyTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable
 story="Currency"
 exclude={[
   'dataAttrs',
 ]}
/>