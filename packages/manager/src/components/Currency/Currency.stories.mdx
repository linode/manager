import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import Currency from './Currency';

<Meta title="Elements/Typography/Currency" component={Currency} />

export const CurrencyTemplate = (args) => <Currency {...args} />;

# Currency

<Canvas>
  <Story
    name="Currency"
    args={{
      quantity: 4.0,
      decimalPlaces: 2,
      wrapInParentheses: false,
    }}
    argTypes={{
      decimalPlaces: {
        control: {
          min: 0,
        },
      },
      wrapInParentheses: {
        description:
          'A boolean used to wrap the currency in parenthesis. This is normally done to indicate a negative amount or balance.',
      },
    }}
  >
    {CurrencyTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Currency" exclude={['dataAttrs']} />
