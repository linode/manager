import DocsLink from './DocsLink';
import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

<Meta title="UI Elements/Link" />

# Docs Link

- The Docs link breaks the pattern of an external link with the position and size of the icon. 
- Consider displaying the title of a key guide or product document as the link instead of the generic “Docs”.

export const Template = (args) => (
  <DocsLink {...args} />
);

<Canvas>
  <Story
    name="Docs Link"
    args={{
      href: "https://linode.com/docs",
      label: undefined,
    }}
    argTypes={{
      href: {
        description: "The destination URL for the link.",
        type: { summary: "string", required: true },
      },
      label: {
        description: "The clickable text of the link.",
        type: { summary: "string", name: "string" },
        table: {
          defaultValue: {
            summary: "Docs",
          },
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>


<ArgsTable story="Docs Link" />