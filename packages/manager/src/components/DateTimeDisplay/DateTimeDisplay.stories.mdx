import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { DateTime } from 'luxon';
import { TimeInterval } from 'src/utilities/formatDate';
import { DateTimeDisplay } from './DateTimeDisplay';

<Meta
  title="Elements/Typography/Date Time Display"
  component={DateTimeDisplay}
/>

export const DateTimeDisplayTemplate = (args) => <DateTimeDisplay {...args} />;

# Date Time Display

<Canvas>
  <Story
    name="Date Time Display"
    args={{
      value: DateTime.now().minus({ day: 3 }).toISO(),
      displayTime: true,
    }}
    argTypes={{
      value: {
        description: 'The date and time string to display',
        control: {
          type: 'date',
        },
      },
      displayTime: {
        description:
          'If true displays time component of the date and time provided.',
      },
      format: {
        description: 'String that specifies a luxon compatible format to use.',
      },
      humanizeCutoff: {
        description:
          'If the date and time provided is within the designated time frame then the date is displayed as a relative date.',
      },
    }}
  >
    {DateTimeDisplayTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Date Time Display" exclude={['className', 'styles']} />
