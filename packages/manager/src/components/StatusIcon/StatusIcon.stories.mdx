import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { StatusIcon } from './StatusIcon';

<Meta title="Components/Status Icons" component={StatusIcon} />

export const StatusIconTemplate = (args) => <StatusIcon {...args} />;

# Status Icons

<Canvas>
  <Story
    name="Status Icons"
    args={{
      status: 'active',
      pulse: false,
    }}
    argTypes={{
      status: {
        description: 'Status of the icon',
        options: ['error', 'other', 'active', 'inactive'],
        control: { type: 'radio' },
      },
    }}
  >
    {StatusIconTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Status Icons" sort="requiredFirst"/>
