import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import Breadcrumb from 'src/components/Breadcrumb';

<Meta
  title="UI Elements/Breadcrumb"
  component={Breadcrumb}
  args={{
    pathname: '/linodes/9872893679817/test/lastcrumb',
  }}
  argTypes={{
    pathname: {
      description: "A string representation of the path of a resource. Each crumb is seperated by a '/' character.",
    },
    crumbOverrides: {
      description: 'An array of objects that can be used to customize any crumb.',
      control: false,
    },
    labelTitle: {
      description: 'A string that can be used to set a custom title for the last crumb.',
    },
    labelOptions: {
      description: 'An object that can be used to configure the final crumb.',
      control: false,
    },
    firstAndLastOnly: {
      description: 'A boolean that if true will only show the first and last crumb',
    },
    onEditHandlers: {
      description: 'An object that can be used to define functions, errors, and crumb title for an editable final crumb',
      control: false,
    },
    removeCrumbX: {
      description: 'A number indicating the position of the crumb to remove. Not zero indexed.',
    },
  }}
/>

export const BreadcrumbTemplate = (args, context) => (
  <Breadcrumb {...args} />
)

# Breadcrumb

## Usage
- Include the current page as the last item in the breadcrumb trail.
- In the breadcrumb trail, the breadcrumb corresponding the the current page should not be a link.

## Basic Breadcrumb

<Canvas>
  <Story name="Basic">
    {BreadcrumbTemplate.bind({})}
  </Story>
</Canvas>


<ArgsTable 
  story="Basic"
  sort="requiredFirst"
  exclude={['className']}
/>
