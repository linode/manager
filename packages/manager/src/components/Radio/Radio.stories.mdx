import FormControlLabel from 'src/components/core/FormControlLabel';
import RadioGroup from 'src/components/core/RadioGroup';
import Radio from './Radio';
import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

<Meta
  title="UI Elements/Radio"
  component={Radio}
/>

export const Template = (args) => {
  return (
    <Radio {...args} />
  );
};

# Radio

<hr />

### Use radio buttons to
- Expose all available options
- Select a single option from a list
 
### Guidelines
- If there are 3 or fewer items to select, use radio buttons rather than drop-down menus.
- If possible, offer a default selection.
- Because radio buttons allow only one choice, make sure that the options are both comprehensive and distinct.
- Let users select an option by clicking on either the button itself or its label to provide as big a target area as possible.
 
### Reasons for A Default Selection
- Expedite tasks
- Give people control and align with their expectations

## Radio

<Canvas>
  <Story name="Radio">
   <Radio />
  </Story>
</Canvas>

## Controled Radio

<Canvas>
  <Story name="Controled Radio">
    <FormControlLabel label="Radio Label" control={<Radio />} />
  </Story>
</Canvas>

## Radio Group

<Canvas>
  <Story name="Radio Group">
    <RadioGroup>
      <FormControlLabel
        label="Disabled"
        control={<Radio disabled />}
      />
      <FormControlLabel label="Default" control={<Radio />} />
    </RadioGroup>
  </Story>
</Canvas>

## Component API

<Canvas>
  <Story
    name="Component API"
    args={{
      checked: false,
      disabled: false,
      color: "primary",
      onChange: () => {},
      disableRipple: false,
      inputProps: undefined,
      inputRef: undefined,
      required: false,
      size: "medium",
    }}
    argTypes={{
      checked: {
        description: 'If `true`, the component is checked.',
      },
      disabled: {
        description: 'If `true`, the radio will be disabled.',
      },
      disableRipple: {
        description: 'If `true`, the ripple effect will be disabled.',
      },
      inputProps: {
        description: 'Attributes applied to the input element. `object`'
      },
      inputRef: {
        	description: 'Pass a ref to the input element. `ref`'
      },
      required: {
        description: 'If true, the input element will be required. `boolean`',
      },
      size: {
        description: 'The size of the radio. small is equivalent to the dense radio styling.`"small" | "medium"`',
        options: ['medium', 'small'],
        control: { type: 'radio' }
      },
      color: {
        description: 'Color of the Radio `"default" | "primary" | "secondary"` Currently, we do not use these variants often. Use `primary`',
        options: ['default', 'primary', 'secondary'],
        control: { type: 'radio' }
      },
      onChange: {
        description: "Callback fired when the state is changed. Signature: `function(event: object) => void` event: The event source of the callback. You can pull out the new value by accessing `event.target.value` (`string`). You can pull out the new checked state by accessing `event.target.checked` (`boolean`)." 
      },
    }}
   >
    {Template.bind({})}
   </Story>
 </Canvas>

<ArgsTable
  story="Component API"
  exclude={[
    'ref',
    'action',
  ]}
/>