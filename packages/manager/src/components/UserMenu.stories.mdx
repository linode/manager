import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { profileFactory } from 'src/factories';
import { UserMenu } from 'src/features/TopMenu/UserMenu';
import { useProfile } from 'src/queries/profile';

<Meta title="Features/Navigation/User Menu" component={UserMenu} />

# User Menu

### Overview

---

- This dropdown contains deep links to all the tabs on both the **My Profile** and **Account** pages.
- If a new tab is added to either the **My Profile** or **Account** pages it must be added to the dropdown.
- If, with the addition of new tabs, the dropdown becomes an unwieldy length consider making the **Account** section a 2 column layout.

export const Template = () => {
  return <UserMenu />;
};

<Canvas>
  <Story name="User Menu">
    <Template />
  </Story>
</Canvas>
