import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { Typography } from 'src/components/Typography';
import { ShowMoreExpansion } from './ShowMoreExpansion';

<Meta
  title="Components/Accordion/Show More Expansion"
  component={ShowMoreExpansion}
/>

# Show More Expansion

export const Template = (args) => (
  <ShowMoreExpansion {...args}>
    <Typography>{args.children}</Typography>
  </ShowMoreExpansion>
);

<Canvas>
  <Story
    name="Show More Expansion"
    args={{
      name: 'Show More Expansion',
      defaultExpanded: false,
      children:
        'Communicating with your Linode is usually done using the secure shell (SSH) protocol. SSH encrypts all of the data transferred between the client application on your computer and the Linode, including passwords and other sensitive information. There are SSH clients available for every desktop operating system.',
    }}
    argTypes={{
      name: {
        description:
          'The text that appears in the clickabe button to show more content',
        type: { summary: 'string', name: 'string', required: true },
      },
      defaultExpanded: {
        description:
          'Whether or not the component should be expanded by default',
        type: { summary: 'boolean', name: 'boolean' },
        table: {
          defaultValue: {
            summary: 'false',
          },
        },
      },
      children: {
        description:
          'The content that will be shown when the component is expanded',
        type: { summary: 'React.ReactNode' },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Show More Expansion" sort="requiredFirst"/>
