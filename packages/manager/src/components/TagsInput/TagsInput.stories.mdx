import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { useArgs } from '@storybook/client-api';
import TagsInput from './TagsInput';

<Meta title="Features/Tags/Tags Input" component={TagsInput} />

# Tags Input

export const Template = (args) => {
  const [args2, updateArgs] = useArgs();
  const onChange = (updatedTags) => {
    updateArgs({ value: updatedTags });
  };
  return <TagsInput value={args2.tags} onChange={onChange} {...args} />;
};

<Canvas>
  <Story
    name="Tags Input"
    args={{
      value: [
        { label: 'tag-1', value: 'tag-1' },
        { label: 'tag-2', value: 'tag-2' },
      ],
      disabled: false,
      hideLabel: false,
      label: '',
      menuPlacement: 'bottom',
      name: '',
      tagError: '',
    }}
    argTypes={{
      disabled: {
        description: 'If true, the component is disabled.',
        table: {
          type: { summary: 'boolean' },
          defaultValue: { summary: 'false' },
        },
      },
      hideLabel: {
        description: 'Removes the label.',
        table: {
          type: { summary: 'boolean' },
          defaultValue: { summary: 'false' },
        },
      },
      label: {
        description: 'The content of the label.',
        table: {
          type: { summary: 'string' },
        },
      },
      menuPlacement: {
        description: 'Changes the placement of the menu.',
        table: {
          defaultValue: { summary: 'bottom' },
        },
      },
      name: {
        description: 'Name attribute of the input field.',
      },
      tagError: {
        description: 'Displays error text below the input field.',
      },
      value: {
        table: {
          type: { summary: 'Tag[]' },
        },
      },
    }}
    height="300px"
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable
  story="Tags Input"
  sort="requiredFirst"
  exclude={['onChange', 'tags']}
/>
