import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import PaginationControls from './PaginationControls';

<Meta title="Components/Pagination Control" component={PaginationControls} />

export const decorators = [
  (Story) => {
    const [page, setPage] = React.useState(1);
    return Story({page, setPage});
  },
];

# Pagination Control

export const Template = (args, context) => 
  <PaginationControls
    count={250} 
    page={context.page}
    onClickHandler={context.setPage}
    pageSize={25} />

<Canvas>
  <Story name="Pagination Control" decorators={decorators}>
    { Template.bind({}) }
  </Story>
</Canvas>
