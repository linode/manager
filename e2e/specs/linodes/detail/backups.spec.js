describe('Name of the group', () => {
    beforeAll(() => {
        // Create a linode here with backups disabled
        // Navigate to Linode Detail Backups section of newly created Linode
    });
});