export const redirect = jest.fn();

export const loginAuthorizePath = jest.fn();

export const checkLogin = jest.fn();

export const expire = jest.fn();

export const expireAndReAuth = jest.fn();

export const start = jest.fn();

export const initialize = jest.fn();
