import * as React from 'react';
import CircleProgress from 'src/components/CircleProgress';

export const ObjectStorageLoadingState: React.FC<{}> = () => {
  return <CircleProgress />;
};

export default ObjectStorageLoadingState;
