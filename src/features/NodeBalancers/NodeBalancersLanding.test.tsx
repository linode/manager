describe('NodeBalancers', () => {
  it('should render 7 columns', () => {
    //
  });
  it('should render Configurations and Delete actions in the Kabob menu', () => {
    //
  });

  it('trigger a confirmation modal when delete is selected', () => {
    //
  });
});
