{
  "name": "root",
  "private": true,
  "devDependencies": {
    "husky": "^3.0.1",
    "lerna": "^3.10.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run precommit --stream --scope linode-manager",
      "pre-push": "lerna run prepush --stream --scope linode-manager"
    }
  },
  "scripts": {
    "up": "yarn && lerna bootstrap && lerna run start --stream --scope linode-manager -- --color",
    "clean": "rm -rf node_modules && lerna clean --yes",
    "test": "lerna run test --stream --scope linode-manager --",
    "docker:e2e": "docker-compose -f integration-test.yml up --exit-code-from manager-e2e",
    "docker:test": "docker build -f Dockerfile . -t 'manager' && docker run -it --rm -v $(pwd)/packages/manager/src:/src/src manager test",
    "docker:local": "docker build -f Dockerfile . -t 'manager' && docker run -it --rm -p 3000:3000 -v $(pwd)/packages/manager/src:/src/src manager start",
    "docker:storybook": "docker build -f Dockerfile . -t 'storybook' && docker run -it --rm -p 6006:6006 -v $(pwd)/packages/manager/src:/src/src storybook storybook",
    "docker:storybook:test": "docker-compose -f packages/manager/storybook-test.yml up --build --exit-code-from storybook-test"
  }
}